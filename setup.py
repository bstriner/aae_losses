from setuptools import find_packages, setup

setup(name='aae_losses',
      version='0.0.1',
      install_requires=[],
      author='Ben Striner',
      url='https://github.com/bstriner/aae_losses',
      packages=find_packages())
